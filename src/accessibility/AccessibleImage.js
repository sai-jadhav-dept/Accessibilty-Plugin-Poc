import AccessibleFilteredImage from './AccessibleFilteredImage';

// Keep a single image implementation in the app module. This export remains for compatibility.
const AccessibleImage = AccessibleFilteredImage;

export default AccessibleImage;
